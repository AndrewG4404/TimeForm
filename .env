# Database configuration
DB_HOST=your_database_host   # e.g., localhost if running locally
DB_USER=your_database_user   # e.g., root
DB_PASSWORD=your_database_password
DB_NAME=historical_events    # or whatever your teammate names the database

# Server configuration (optional, if needed)
PORT=3000  # Specify if you want to control the server port
